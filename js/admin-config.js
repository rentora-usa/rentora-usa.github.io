// Fill this in once you've deployed admin-worker/ (see README §11). Leave
// blank and the Users tab in admin.html will say so rather than silently
// failing — listing moderation and everything else in the staff panel
// works either way, since only account management needs this Worker.
export const ADMIN_WORKER_URL = "rentora-admin.jonnyschoen29.workers.dev";
